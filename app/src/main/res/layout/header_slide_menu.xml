<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_header_layout"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:background="@drawable/pink_background"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="16dp">


    <android.support.v7.widget.CardView
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:cardCornerRadius="32dp">

        <ImageView
            android:id="@+id/slider_menu_avatar"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:contentDescription="@string/spare_time_for_u"
            android:scaleType="centerCrop"
            app:srcCompat="@mipmap/ic_launcher_round" />

    </android.support.v7.widget.CardView>


    <TextView
        android:id="@+id/slider_menu_nick_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/material_background"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/slider_menu_signature"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:maxLines="3"
        android:text="@string/spare_time_for_u"
        android:textColor="@color/material_background"
        android:textStyle="italic" />


</LinearLayout>




















